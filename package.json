{
  "name": "reconciliation",
  "version": "0.0.0",
  "private": true,
  "description": "Nigerian fintech reconciliation engine",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "bench": "npm run build && node apps/pipeline/dist/main.js bench",
    "test": "npm run build && node --test \"packages/*/dist/**/*.test.js\" \"apps/*/dist/**/*.test.js\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22"
  }
}
