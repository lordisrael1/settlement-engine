# Configuration arrives as environment, never baked into the image.
# docker-compose.yml sets this for you; copy to .env only for running outside Docker.

DATABASE_URL=postgres://recon:recon@localhost:5432/recon
