{
  "name": "@recon/ingest",
  "version": "0.0.0",
  "private": true,
  "description": "The anti-corruption boundary. Turns foreign payloads into canonical events, once, at the edge.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@recon/canon": "0.0.0",
    "@pay-normalize/core": "^0.1.2",
    "@pay-normalize/flutterwave": "^0.1.2",
    "@pay-normalize/monnify": "^0.1.2",
    "@pay-normalize/nomba": "^0.1.2",
    "@pay-normalize/paystack": "^0.1.4"
  }
}
